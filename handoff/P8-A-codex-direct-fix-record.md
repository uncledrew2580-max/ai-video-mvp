# P8-A Handoff / Review Record

Date: 2026-05-27
RC: AI-Video-Mac-MVP-20260527-2223 (generated by Claude Code in this session)

---

## C. Codex Direct Fix Record

### 修复内容

**final-video 单镜头视频展示修复（Codex direct fix）**

- 修复人：Codex（非 Claude Code）
- 修复时间：2026-05-26（1901 RC 之前）
- 未经 Claude Code 执行，直接由 Codex 修改源码

**修改文件：**
- `版本测试/serve-review-assets.mjs`

**修改原因：**
- 1901 RC 中 final-video 页面 6 个单镜头视频无法展示和播放
- `findShotVideo` 函数未优先使用 `progress.shots[].video_path`（绝对路径）
- 导致 dist 模式下找不到 `~/Downloads/n8n视频` 中的视频文件

**核心修复逻辑：**
- `findShotVideo` 函数现在优先读取 `shotProg.video_path`（progress JSON 中的绝对路径）
- 只有当 progress 中没有有效路径时，才回落到扫描 `VIDEO_OUTPUT_ROOT`

**验证证据：**
- P7 完整测试通过：6 个单镜头视频均可展示和播放
- final-video 状态：总计 6、完成 6、进行中 0、失败 0
- 最终合成视频正常生成：`~/Library/Application Support/AI Video/workflow-data/.n8n-local-cache/final-video/final_proj_*.mp4`

**残留风险：**
- source/dev 模式下，`VIDEO_OUTPUT_ROOT` 原本指向 `~/Downloads/n8n视频`，而 WF03 实际写入 `PROJECT_ROOT/.n8n-local-cache/videos`；路径不一致，source 模式视频扫描可能失败
- packaged/dist 模式已修正：P8-A Claude Code fix 将 `VIDEO_OUTPUT_ROOT` 统一为 `CACHE_ROOT/videos`，与 WF03 输出路径对齐

---

## A. WF03 / Veo 链路修复（P7-D2 Claude Code 执行）

### 已固化修复
1. WF03 全部 Code 节点去除 `execFileSync` 和 `Atomics.wait`，改为 async `spawn`
2. 节点：`逐镜视频_API_占位`、`Veo查询任务状态`、`Veo下载视频`、`Veo结果汇总`
3. `Veo下载视频` outputDir：`~/Downloads/n8n视频` → `DATA_ROOT/.n8n-local-cache/videos`（已在源码中）
4. `Veo结果汇总` videoDir：同上；mergedPath 写入 `concatDir`（App Support final-video）
5. 无旧代理 `127.0.0.1:6478` 引用
6. 无硬编码源码目录（使用 `os.homedir()` + 动态路径）

### 两个修改文件
- `正式导入文件/iteration-v1/n8n03.json`（源码）
- `release-candidates/AI-Video-Mac-MVP-20260526-1901/.../正式导入文件/iteration-v1/n8n03.json`（已同步）

---

## B. final-video 展示修复（P8-A Claude Code 执行）

1. `VIDEO_OUTPUT_ROOT`：去除 `~/Downloads/n8n视频` fallback，统一为 `CACHE_ROOT/videos`
2. `/local-file` 安全白名单保留：`CACHE_ROOT/videos`、`CACHE_ROOT/final-video`
3. `.mp4`/`.mov`/`.webm` MIME 保留
4. `findShotVideo` 优先 `progress.shots[].video_path`（Codex fix，已在源码中）
5. 状态页 `视频目录` 文案：dist 模式显示 App Support 路径（已正确）

**修改文件：**
- `版本测试/serve-review-assets.mjs`（VIDEO_OUTPUT_ROOT 一行）

---

## D. 一致性验证

| 文件 | 状态 |
|------|------|
| 源码 `n8n03.json` | 已修复（async spawn） |
| 1901 RC bundle `n8n03.json` | 已同步 |
| 新 RC bundle `n8n03.json` | 从源码复制 |
| 源码 `serve-review-assets.mjs` | VIDEO_OUTPUT_ROOT 已修复 |
| 新 RC bundle `serve-review-assets.mjs` | 从源码复制 |
| App Support n8n DB | sync_iteration_v1 已执行（P7-D3）|

---

## P7 完整闭环记录

- WF01 创意方向：OK
- WF02a 脚本框架：OK
- WF02b Nano Banana 分镜图：OK
- WF03 Veo 视频（6 镜头）：OK
- 最终合成视频：OK
  - 路径：`~/Library/Application Support/AI Video/workflow-data/.n8n-local-cache/final-video/final_proj_1779806424534_1779886747292.mp4`
- final-video 页面展示：OK（6 个单镜头 + 最终成片）

---

## 下一步

- P8-B PREPACKAGE-CHECK（Codex Review 通过后）
- 新 macOS 用户测试
- 不建议进入 Windows 版本
- 不建议现在生成 DMG
