# P8-D 交付说明 — AI-Video-Mac-MVP-20260528-0409

生成时间: 2026-05-28  
工单编号: P8-D → P8-D-CLEAN  
状态: ✅ 候选包已清理，等待 Codex Review

---

## A. P8-C 修复固化记录（Codex 直接修复，已写入源文件）

### FIX1 — WF01 `require('process')` 任务沙箱修复
- 文件: `正式导入文件/iteration-v1/n8n01.json`
- 节点: `Prompt Library`
- 修复: bare `process.env` → `(require('process').env || {})`

### FIX2 — WF02a `require('process')` 修复
- 文件: `正式导入文件/iteration-v1/n8n02a.json`
- 节点: `Prompt Library`
- 修复: 同 FIX1

### FIX3 — WF02b `require('process')` 修复
- 文件: `正式导入文件/iteration-v1/n8n02b.json`
- 节点: `读取脚本上下文`
- 修复: 同 FIX1

### FIX4 — WF03 `逐镜视频_API_占位` readLocalConfig 缺失 candidates 数组
- 文件: `正式导入文件/iteration-v1/n8n03.json`
- 节点: `逐镜视频_API_占位`
- 根因: readLocalConfig() 内引用 `candidates` 变量但从未声明
- 修复: 添加 `const candidates = [...]` 包含 App Support 路径
- MD5 (fix后): `d1d71267740e12bb37335d709b2a7453`

### FIX5 — WF03 `Veo查询任务状态` readLocalConfig 缺少 App Support 路径
- 文件: `正式导入文件/iteration-v1/n8n03.json`
- 节点: `Veo查询任务状态`
- 根因: candidates 数组中缺少 `~/Library/Application Support/AI Video/config/local-config.json`，dist 模式下 projectRoot() 返回 bundle 路径，配置读取失败 → API Key 为空
- 修复: 添加 App Support 路径为 candidates 第二候选（env var 之后）
- MD5 (fix后): `d1d71267740e12bb37335d709b2a7453`（同 FIX4 同文件）

---

## B. P8-C 验证成功证据

- **项目**: proj_1779906391739  
- **执行**: exec 12，WF01→WF02a→WF02b→WF03 完整链路  
- **结果**: 6/6 镜头视频生成成功（kie_veo31_proj_1779906391739_shot_{1-6}_*.mp4，1.1–2.6MB 每个）  
- **合成**: final_proj_1779906391739_1779910794577.mp4 (9.8MB)  
- **final-video 页面**: HTTP 200，6 个 badge-done，6 个 `<video>` 元素，全部 HTTP 200

---

## C. 新 RC 信息

| 字段 | 值 |
|------|----|
| 候选包名称 | `AI-Video-Mac-MVP-20260528-0409` |
| 基于 | `AI-Video-Mac-MVP-20260527-2333`（P8-C 验证通过版本）|
| 位置 | `release-candidates/AI-Video-Mac-MVP-20260528-0409/AI Video.app` |
| 签名 | ad-hoc codesign `--force --deep --sign -`（P8-D-CLEAN 后重新签名）✅ |
| 大小 | ~3.0 GB |

---

## D. Workflow JSON 哈希（源 == RC 捆绑）

| 文件 | MD5 |
|------|-----|
| n8n01.json | `009beada9e430a158857c2b18c243e09` |
| n8n02a.json | `03ef239ab5819c155322b92b8896b3c5` |
| n8n02b.json | `75716680fd2f059a14c0e83ca42c8013` |
| n8n03.json | `d1d71267740e12bb37335d709b2a7453` |

serve-review-assets.mjs: `bac986d937381e07c50795d0f545de46`（源 == RC）

---

## E. D.5/D.6 状态说明

**D.5 (status page 视频目录)**  
- Line 3361: `<code>${htmlEscape(VIDEO_OUTPUT_ROOT)}</code>`  
- `VIDEO_OUTPUT_ROOT` = `process.env.VIDEO_OUTPUT_DIR || path.join(CACHE_ROOT, 'videos')`  
- dist 模式下 CACHE_ROOT = `~/Library/Application Support/AI Video/`，路径正确 ✅  
- `serve-review-assets.mjs` 中无 `/Users/drew` 硬编码（`grep -c /Users/drew` = 0）

**D.6 (config page 输出目录 placeholder 文字)**  
- Line 3796: `placeholder="~/Downloads/tiktok-videos"` — 仅为 input 提示文字  
- Line 3802-3803: Mac/Windows 示例文字  
- 这是 UI 配置页面的说明文本，不是运行时 fallback 路径  
- 实际运行时输出目录由 `VIDEO_OUTPUT_ROOT` 决定（见 D.5）  
- **建议**: 下一版可将示例文字改为 `~/Library/Application Support/AI Video/videos`，但不阻塞当前 RC

---

## F. 模型冻结确认

| 模型类型 | 冻结值 | RC 状态 |
|----------|--------|---------|
| text_model | gemini-3.1-pro | ✅ |
| image_model | nano-banana-pro | ✅ |
| video_model | veo3_lite | ✅ |
| 旧 flash 模型 | 无 | ✅ 已消除（workflow JSON + serve-review-assets.mjs 均无）|
| gemini-2.5-flash | node_modules 第三方库（langsmith/n8n chat-hub）| ℹ️ 非项目模型选择，不影响业务链路 |

---

## G. P8-D-CLEAN 清理记录（Codex Review 要求）

Codex Review 发现以下问题并要求 P8-D-CLEAN 工单修复：

### 已删除

| 文件 | 原因 |
|------|------|
| `正式导入文件/iteration-v1/n8n02b.json.bak-20260526-175938` | 旧备份文件，含旧模型/旧 fallback 文本；不是正式 workflow JSON |
| `docs/tiktok_ai_video_workflow_usage.md`（RC 内） | 非运行必需文档，含 7 处 `/Users/drew/Downloads/tiktok-n8n-workflow-pack` 绝对路径 |
| `docs/CLIENT_PACKAGING_PLAN.md`（RC 内） | 非运行必需文档，含 `/Downloads/tiktok-n8n-workflow-pack` 路径 |

### 未修改但已分类说明

| 文件 | 分类 | `/Users/drew` 性质 | 运行依赖 |
|------|------|-------------------|---------|
| `sync_iteration_v1_workflows_to_db.mjs` | 运行时 admin 工具（sync-workflows 触发） | 路径检测字符串（`if (raw.includes('/Users/drew')) throw`） | 是（serve-review-assets 触发） |
| `scripts/bootstrap-ai-video-workflows.mjs` | Admin 脚本 | 路径检测字符串（同上）| 否（Electron main 未调用） |
| `scripts/check-path-isolation.mjs` | 开发校验脚本 | 注释文本（`No literal /Users/drew`）| 否 |
| `正式导入文件/iteration-v1/MANIFEST.md` | 文档 | grep 命令模式（`grep -l '/Users/drew'`）| 否 |

### node_modules build metadata

- **49 个** build metadata 文件（`.mk` / `config.gypi` / `.o.d` / `Makefile` / `Release/.deps/`）含 `/Users/drew`
- 均为 native addon 编译产物（`@sentry-internal`、`cpu-features`、`@parcel/watcher`、`isolated-vm`、`ssh2`）
- **不影响运行逻辑**：node_modules 中零个 runtime JS 文件含 `/Users/drew`
- 风险级别：**低**（编译元数据，非可执行路径）
- 全包严格 `grep /Users/drew` 仍会命中；后续正式交付可通过 `--rebuild` + 干净环境 package 消除

---

## H. P8-D 启动检查说明（重要更正）

**P8-D / P8-D-CLEAN 仅做静态检查和签名，未做新候选包启动验证。**

具体未做项：
- 未启动 `AI-Video-Mac-MVP-20260528-0409/AI Video.app`
- 未验证 Electron 启动正常
- 未验证 20260528-0409 候选包 Electron 启动、UI 18788、n8n 5678、dist App Support 隔离
- 未验证 dist 模式下 App Support 数据隔离
- 未跑新包完整 WF01→WF02a→WF02b→WF03 链路

以上启动验证放入 **P8-E**。

---

## I. 当前建议

| 建议 | 理由 |
|------|------|
| ✅ 建议进入 Codex Review | P8-D-CLEAN 已处理 Codex 指出的全部 4 个未通过点 |
| 待 Codex Review 通过后，进入 P8-E PREPACKAGE-CHECK | 无模型启动预检：启动新 RC、验证 UI 18788 / n8n 5678 / 路径隔离 / 配置读取 / workflow active 状态 / 无新增 execution；不提交表单，不触发 Gemini/Nano Banana/Veo；完整业务链路属于后续新用户测试，另行确认 |
| 勿生成 DMG | 直到 P8-E 验收通过 |
| 勿 GitHub 上传 | 同上 |
